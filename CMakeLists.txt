cmake_minimum_required(VERSION 2.8)
project( buildzr )
add_compile_options(-std=c++11)
find_package( Eigen REQUIRED )
include_directories( "../eigen" )
add_executable( buildzr src/main.cpp )
#target_link_libraries( anggel ${Eigen_LIBS} )
